class Pipeline {
  constructor(id, code, type, name, address) {
    this.id = id;
		this.code = code;
		this.type = type;
		this.name = name;
		this.address = address; 
  }
}

export default Pipeline;