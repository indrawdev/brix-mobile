class Client {
  constructor(client_id, client_name, client_type, client_address1) {
    this.client_id = client_id;
		this.client_name = client_name;
		this.client_type = client_type;
		this.client_address = client_address1;
  }
}

export default Client;